{
  "name": "GPU Conda Dev Container",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "runArgs": [
    "--gpus=all",
    "--shm-size=16g"
  ],
  "forwardPorts": [8888],
    "postStartCommand": "micromamba run -n fastbook python -m jupyter lab --ip=0.0.0.0 --port=8888 --no-browser --allow-root"
}